# Database Configuration
# MongoDB connection string (local or MongoDB Atlas)
# Local: mongodb://localhost:27017/wholesale_inventory
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/wholesale_inventory
MONGO_URI=mongodb+srv://ashmit123:g1Ik2afR9nNEbUjT@cluster0.vk0zthd.mongodb.net/wholesale_inventory?retryWrites=true&w=majority
# Authentication Configuration
# JWT secret key for signing tokens (use a strong random string, min 32 characters)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars

# JWT token expiration time (e.g., 30d for 30 days, 7d for 7 days)
JWT_EXPIRE=30d

# Server Configuration
# Server port number
PORT=5000

# Environment mode (development or production)
NODE_ENV=development

# CORS Configuration
# Frontend URL for CORS whitelist (Vite dev server typically runs on :5173)
CLIENT_URL=http://localhost:5173
